import { Request, Response } from "express";
import { catchAsync } from "../utils/catchAsync";


//! updateInventory - Post (Goal: Physically place a product into a specific store.)
export const updateInventory = catchAsync(async (res: Response, req: Request) =>{

})


//! getStoreInventory - GET (Goal: See everything currently sitting inside one specific Dark Store.)
export const getStoreInventory = catchAsync(async (res: Response, req: Request) => {
    
});